spring:
  application:
    name: gulimall-gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        prefix: gulimall-gateway
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:8848
  thymeleaf:
    cache: false

